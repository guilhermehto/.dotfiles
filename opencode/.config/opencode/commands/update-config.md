---
description: Sync agents/commands/skills from ~/.dotfiles/ai into ~/.config/opencode
agent: build
---

Sync output:

!`$HOME/.config/opencode/bin/opencode-sync-ai`

Summarize the actions in 2-4 lines. Call out any conflicts, skipped files,
or pruned symlinks. If the script exited non-zero, surface that prominently
and do not claim success.
